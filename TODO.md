# TODO - eventify (JPA + PostgreSQL resources)

## Pendientes
- [x] Agregar `eventify/src/main/resources/application.properties` con configuración base JPA + PostgreSQL (placeholders).
- [ ] Corregir errores de compilación: `VenueService` no tiene los métodos `getAllVenues()`, `getVenueById(int)`, `addVenue(Venue)` usados por `VenueController`.

